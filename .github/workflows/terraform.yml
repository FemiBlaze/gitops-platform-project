name: Terraform Infrastructure

on:
  push:
    branches: [ main ]
    paths:
      - "infra/**"
    paths-ignore:
      - ".github/**"
  workflow_dispatch:

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - name: Terraform placeholder
        run: echo "Terraform intentionally disabled until infra is ready."

